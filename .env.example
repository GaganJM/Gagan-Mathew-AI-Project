ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Google Cloud OAuth Client (type: Desktop app) — used for both Drive upload
# and sending the summary email via Gmail API under the same consent.
GOOGLE_CLIENT_ID=xxxxxxxxxxxx.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=xxxxxxxxxxxxxxxxxxxxxxxx

# Where the reviewer's summary email is sent
EMAIL_RECIPIENT=gaganmathew00@gmail.com

# Optional overrides
# UPLOAD_ROOT=./uploads
# EXCEL_LOG_PATH=./data/submissions.xlsx
# MAX_UPLOAD_MB=50
# PORT=5000
# GOOGLE_TOKEN_PATH=./server/google_token.json

# Set FLASK_DEBUG=false before exposing the app publicly (e.g. via a tunnel).
# FLASK_DEBUG=true

# Set both before exposing the app publicly — requires this username/password
# via HTTP Basic Auth on every request. Leave unset for normal local dev.
# DEMO_USERNAME=
# DEMO_PASSWORD=
